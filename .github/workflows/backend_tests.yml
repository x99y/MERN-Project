name: Run Backend Tests

on: workflow_call

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install
        working-directory: ./Backend

      - name: Run tests and save log
        # ensure that the report is output to the backend_test_log.txt, as well as in the actions console 
        run: |
          mkdir -p ./test_logs
          npx jest --reporters=default --silent=false --json --outputFile=./test_logs/backend_test_log.json
        working-directory: ./Backend
        env:
          CI: true

      - name: Upload test logs
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-logs
          path: ./Backend/test_logs/backend_test_log.txt